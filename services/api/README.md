# GiziGo API

FastAPI service hosting the meal-plan optimizer.

Bootstrap from the repo root:

```bash
make bootstrap
make api-dev
```

Endpoints:
- `POST /v1/optimize`
- `POST /v1/sensitivity`
- `POST /v1/humanize`
- `GET /v1/health`
